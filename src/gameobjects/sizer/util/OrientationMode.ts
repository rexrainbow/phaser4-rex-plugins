export enum OrientationMode {
    x = 0,
    h = 0,
    horizontal = 0,
    'left-to-right' = 0,
    y = 1,
    v = 1,
    vertical = 1,
    'top-to-bottom' = 1
}

export type OrientationModeString =
    'x' | 'h' | 'horizontal' | 'left-to-right' |
    'y' | 'v' | 'vertical' | 'top-to-bottom';