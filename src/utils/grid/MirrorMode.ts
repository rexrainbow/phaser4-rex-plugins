export enum MirrorMode {
    x = 1,
    horizontal = 1,
    h = 1,
    y = 2,
    vertical = 2,
    v = 2,
    xy = 3,
}

export type MirrorModeString = 'x' | 'y' | 'xy' | 'vertical' | 'horizontal' | 'v' | 'h';