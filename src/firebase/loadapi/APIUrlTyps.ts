export type APIUrlTyps = {
    app: string,

    auth?: string,
    database?: string,
    firestore?: string,

    analytics?: string,
    functions?: string,
    messaging?: string,
    performance?: string,
    'remote-config'?: string
}